node_modules
backend-mocks/google-sheets-express-app/credentials.json

# Environment files — never commit secrets; use .env.example as a template
.env
.env.local
.env.*.local
workspace-otp-server/.env
